<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Mapper.PayOrderMapper">

    <!-- 日终结算：按支付方式分组统计 -->
    <select id="dailyRevenueStats" resultType="java.util.HashMap">
        SELECT
            COALESCE(pay_method, 'UNKNOWN') as pay_method,
            COUNT(*) as order_count,
            COALESCE(SUM(total_amount), 0) as total_amount
        FROM pay_order
        WHERE DATE(pay_time) = #{date}
          AND status = 1
        GROUP BY pay_method
    </select>

    <!-- 多维度营收统计 -->
    <select id="revenueStats" resultType="java.util.HashMap">
        SELECT
            COUNT(*) as total_orders,
            COALESCE(SUM(total_amount), 0) as total_revenue,
            COALESCE(AVG(total_amount), 0) as avg_amount
        FROM pay_order
        WHERE status = 1
        <if test="startDate != null and startDate != ''">
            AND DATE(pay_time) &gt;= #{startDate}
        </if>
        <if test="endDate != null and endDate != ''">
            AND DATE(pay_time) &lt;= #{endDate}
        </if>
        <if test="deptId != null">
            AND dept_id = #{deptId}
        </if>
        <if test="doctorId != null">
            AND doctor_id = #{doctorId}
        </if>
        <if test="payMethod != null and payMethod != ''">
            AND pay_method = #{payMethod}
        </if>
    </select>

    <!-- 统计某日已缴费订单数 -->
    <select id="countPaidByDate" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM pay_order
        WHERE DATE(pay_time) = #{date}
          AND status = 1
    </select>

    <!-- 统计某日已缴费总金额 -->
    <select id="sumPaidByDate" resultType="java.math.BigDecimal">
        SELECT COALESCE(SUM(total_amount), 0)
        FROM pay_order
        WHERE DATE(pay_time) = #{date}
          AND status = 1
    </select>

</mapper>
