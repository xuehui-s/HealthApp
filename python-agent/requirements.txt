# ============================================================
# 医疗智能Agent v2 - Python 依赖（全部为运行必需，pip install -r requirements.txt）
# 设计原则：核心运行时零重型依赖（无 LangChain/向量库强制项），
#          企业级能力（重试/限流/指标/护栏/可观测）均为标准库或轻量库实现
# ============================================================

# Web 框架
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2

# LLM 调用（OpenAI 兼容协议：DeepSeek/通义/豆包/GLM 等）
openai==1.51.0
httpx==0.27.2
tenacity==9.0.0

# 数据库 / 缓存
pymysql==1.1.1
redis==5.0.8

# 中文分词（BM25 检索）
jieba==0.42.1

# 工具
loguru==0.7.2
python-dotenv==1.0.1

# 测试
pytest==8.3.3
pytest-asyncio==0.24.0

# ------------------------------------------------------------
# 可选增强（默认不安装）：
#   chromadb              —— 启用向量检索，与 BM25 组成混合检索（RAG 增强）
#   sentence-transformers —— 本地中文 embedding 模型
#   langchain / langgraph —— 如需迁移到框架编排可安装（当前为自研 ReAct 编排）
# ------------------------------------------------------------
